{
  "name": "litellm-devcontainer",
  "version": "1.0.0",
  "description": "LiteLLM + Ollama Dev Container Feature — TypeScript tooling",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "biome lint ./scripts",
    "format": "biome format ./scripts --write",
    "check": "biome check ./scripts",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.5.0"
  }
}
